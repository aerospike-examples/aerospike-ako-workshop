# Aerospike license — instructor-supplied, never commit
**/secrets/features.conf

# Local env overrides and runtime artifacts
.cursor/
**/.kube/
**/*.env
!**/*.env.example
**/credentials.json
**/*kubeconfig*
**/*.pem
**/*.key
workshop/.vendor/
